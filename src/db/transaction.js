export async function createTransaction(data) {
  // To create a transaction and add it in the db
}

export async function getTransaction(id) {
  // To get transaction details 
}

export async function udpateTransaction(id, data) {
  // Id is to know what transaction to update and data is for what to update
}

export async function getUserTransactions() {
  // to get user transaction for a query
}